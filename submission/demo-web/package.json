{
  "name": "t3-demo-recorder",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Records the Gatekeeper Agent demo (HTML terminal player) to a finished MP4 via Playwright + ffmpeg-static, muxing the voice-over track automatically.",
  "scripts": {
    "browser": "playwright install chromium",
    "record": "node record.mjs"
  },
  "dependencies": {
    "ffmpeg-static": "^5.3.0",
    "playwright": "^1.61.0"
  }
}
